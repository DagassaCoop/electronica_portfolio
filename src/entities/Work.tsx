export interface IWork {
  id: string;
  title: string;
  description: string;
  img: string;
  date: Date;
  link: string;
}

export interface IWorkPreview {
  id: string;
  title: string;
  img: string;
}
